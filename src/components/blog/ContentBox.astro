---
export interface Props {
  children: any;
  className?: string;
}

const { children, className = "" } = Astro.props;
---

<div class={`bg-white dark:bg-gray-900 rounded-xl p-6 mb-8 shadow-md ${className}`}>
  <slot />
</div> 