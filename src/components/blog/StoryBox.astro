---
export interface Props {
  gradient: string;
  children: any;
}

const { gradient, children } = Astro.props;
---

<div class={`bg-gradient-to-r ${gradient} rounded-xl p-6 mb-8 shadow-md`}>
  <slot />
</div> 