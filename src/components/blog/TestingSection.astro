---
export interface Props {
  title: string;
  description: string;
  steps: {
    number: number;
    title: string;
    description: string;
    color: string;
  }[];
  tip: {
    text: string;
  };
}

const { title, description, steps, tip } = Astro.props;

const colorClasses: Record<string, string> = {
  green: 'from-green-400 to-green-500',
  blue: 'from-blue-400 to-blue-500',
  purple: 'from-purple-400 to-purple-500',
  orange: 'from-orange-400 to-red-500',
  teal: 'from-teal-400 to-cyan-500',
  pink: 'from-pink-400 to-red-500'
};
---

<div class="bg-gradient-to-r from-amber-50 to-orange-50 dark:from-amber-900/20 dark:to-orange-900/20 rounded-3xl p-8 mt-16 mb-12 shadow-md border-r-4 border-amber-500">
  <div class="flex items-start">
    <div class="feature-icon ml-6 flex-shrink-0">
      🎯
    </div>
    <div>
      <h3 class="text-2xl font-bold text-amber-800 dark:text-amber-300 mb-4">{title}</h3>

      <div class="text-lg text-gray-800 dark:text-gray-200 leading-relaxed mb-6">
        {description}
      </div>

      <div class="grid md:grid-cols-2 gap-6">
        {steps.map((step) => (
          <div class="flex items-start">
            <div class="w-12 h-12 bg-gradient-to-br from-purple-400 to-blue-500 rounded-full flex items-center justify-center text-white text-lg font-bold ml-4 mt-1 flex-shrink-0 shadow-md">
              {step.number}
            </div>
            <div>
              <div class="font-semibold text-gray-900 dark:text-gray-100 mb-1">{step.title}</div>
              <div class="text-sm text-gray-600 dark:text-gray-400">{step.description}</div>
            </div>
          </div>
        ))}
      </div>

      <div class="bg-gradient-to-r from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-xl p-4 border-r-4 border-green-500 mt-6">
        <div class="flex items-center">
          <span class="text-2xl ml-3">💡</span>
          <div class="text-lg font-semibold text-green-800 dark:text-green-300">
            {tip.text}
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 